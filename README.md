# Radar
Arduino-based radar that sweeps an ultrasonic sensor on a servo from 15° to 165°, measures object distance in real time, and streams angle–distance data over Serial. View readings in the Arduino Serial Monitor or create a live radar display using Processing for a fun, interactive visual.
